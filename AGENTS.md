# 版本与提交规则

- 版本号由用户主动、明确指定。代理不得自行建议、推算或递增下一版本号。
- 用户未指定版本号时，保留当前版本，继续实现和验证；不得把功能完成、编译通过或准备提交当作修改版本号的授权。
- 用户明确指定后，再同步固件版本、README 和 CHANGELOG。允许跨版本号，不能假定必须逐号递增。
- 版本提交沿用现有格式：`vX.Y.Z: type(scope) 中文说明`，其中版本号必须使用用户明确指定的值。
- 提交只包含任务相关文件；已有本地配置修改和临时验证产物不得顺带提交。
